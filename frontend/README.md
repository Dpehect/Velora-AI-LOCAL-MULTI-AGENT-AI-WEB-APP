# Velora — Frontend

Next.js 15 + Tailwind + TypeScript. Deploy: **Vercel**.

```bash
cd frontend
npm install
npm run dev
```

Scaffold only — UI logic not implemented yet.

Set `NEXT_PUBLIC_API_URL` in `.env.local` when connecting to the backend.
