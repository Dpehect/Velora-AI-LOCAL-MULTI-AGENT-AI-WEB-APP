# --- Velora Frontend (copy to .env.local) ---
# Backend base URL only — no trailing slash.
# This is the sole coupling point to the backend package.

# Local
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Production (Fly.io example)
# NEXT_PUBLIC_API_URL=https://velora-ai-lab-api.fly.dev
