{
  "name": "velora-ai-lab",
  "private": true,
  "version": "1.0.0",
  "description": "Velora AI Lab — static landing (Vercel) + local multi-agent Python app",
  "scripts": {
    "build": "node scripts/vercel-build.js",
    "vercel-build": "node scripts/vercel-build.js"
  },
  "engines": {
    "node": ">=18"
  }
}
